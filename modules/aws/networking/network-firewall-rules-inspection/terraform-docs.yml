formatter: markdown

output:
  file: README.md
  mode: inject

content: |
  # Terraform Module for - network-firewall-rules-inspection
  ...
  {{ .Providers }}
  {{ .Requirements }}
  {{ .Resources }}
  {{ .Inputs }}
  {{ .Outputs }}
  {{ .Footer }}
settings:
  sections:
    - header
    - providers
    - requirements
    - resources
    - inputs
    - outputs
    - footer
